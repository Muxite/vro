# ESP32 Firmware

This directory contains the ESP32 firmware project.

To build and flash:
1. Set up ESP-IDF environment: `../scripts/setup_esp_idf.sh`
2. Build the project: `idf.py build`
3. Flash from Pi: `../scripts/flash_esp32.sh [port] [baud]`
